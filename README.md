# config.nvim

HTTPS:
```bash
git clone https://github.com/eeemi/config.nvim.git "${XDG_CONFIG_HOME:-$HOME/.config}"/nvim
```

SSH:
```bash
git clone git@github.com:eeemi/config.nvim.git "${XDG_CONFIG_HOME:-$HOME/.config}"/nvim
```

